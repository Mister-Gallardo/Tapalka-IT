import { Typography } from "@mui/material";

function MobileErr() {
  return (
    <Typography
      sx={{
        display: "block",
        fontSize: "32px",
        fontWeight: "bolder",
        color: "white",
        textAlign: "center",
        marginTop: "130px",
      }}
    >
      Продолжите на мобильном устройстве
    </Typography>
  );
}

export default MobileErr;
